[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ebb"
version = "0.1.0"
description = "A 2D physics simulation that runs backwards bit-for-bit without stored frames."
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
packages = ["ebb"]
